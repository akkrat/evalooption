# Remaining budget-exhausted continuations

Resume the two exhausted library-suite runs and the exhausted final calibration pilot. Actual Budget originals already have terminal continuations and are not repeated. Blocked runs are not selected. Pilot evidence stays labeled calibration.

Preserve original artifacts and copy source workspaces with their snapshot Git metadata. Resume the same Luna medium developer and Luna xhigh agent sessions with an explicit new cwd. For nested Gennady dispatches, resume the interrupted leaf first, then return its actual response to each suspended parent session. The two selected nested chains each have exactly one pending child at every level; reject ambiguous chains. Do not provide hidden tests, historical solutions, independent review results or suggested fixes. Keep existing models, workflows and grading adapters.

Additional limits per selected run: 60 minutes per CLI call, four hours per run, 80 million input-plus-output tokens checked between calls, 32 questions/revisions per stage, 64 new dispatches. Preserve old stage durations as inherited context; charge only new turns. Report incremental and cumulative effort separately. No quota reset is authorized or consumed.

Validate relocation and denial of the old checkout with a real Luna xhigh session before execution. Freeze the continuation engine and record original evidence hashes before resuming. Regrade submitted code and perform the existing blind baseline-context review after each workflow ends. Do not replace original results or treat continuations as independent repetitions.
