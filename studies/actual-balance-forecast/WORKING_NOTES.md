# Continuation state — 2026-09-10, about 01:47 Moscow

Original matrix is COMPLETED, finalized and audited: `runs/suite/COMPARISON.md`, MEASUREMENTS.json, AUDIT.json. Do not rerun its 12 trials or alter its frozen engine. No reset credit authorized/used.

## Separate Actual Budget study

User-authorized additive four-approach application task from the side conversation delegation. PR #7310 explicitly AI-assisted; no human-only claim. Base/fix, brief and runtime pins are in study.json/task.md/runtime.json. All four actors use Luna medium simulated developer and Luna xhigh agents. Workflow packages remain pinned original versions. Own runner/adapter copies do not change approach_eval engine.

### Verified

- Node 22.22.2, Yarn 4.13.0, full immutable original lock, Playwright 1.59.1/Chromium, Vitest 4.1.4; separately Yarn-locked coverage provider in `.eval-cache/actual-instrumentation`.
- Base core622/web603 pass, 3 skipped. Gold core637/web610 pass, 3 skipped. No original regressions. Base/gold production builds, types and lint pass under evaluator Seatbelt isolation.
- Hidden historical tests: 15 core + 7 web pass on gold; base fails private-module compatibility as expected. These are separate from independent product behavior.
- Browser checks use pre-existing RPCs to seed an EMPTY budget with one1000 account and monthly200 debit starting2017-01-15; assert annual minimum-1400/dateDec15, daily agreement, 3-month minimum400/dateMarch15, persisted settings, posted50 debit ->350. 7 feature checks +startup pass on gold, fail on base. Dates frozen in served browser+worker JS; schedule totals discriminate wrong backend clock.
- Gold screenshots visually inspected. Added1800ms screenshot settle for Recharts animation; `validation/gold-browser-stable-render` passes and shows the plotted line. No Linux pixel snapshot claim.
- Codex preflight `preflight/20260910-013237` passed hidden-reference/source/dependency/Git protections and localhost allow / external-domain rejection. CLI requires network_proxy + allow_local_binding true. No external domains allowed.
- Build/browser preflight `preflight/build-20260910-014055` PASSED build, 15 schedule tests,5 web tests, controlled browser launch, hidden-read/dependency/Git-write/external-network denial.
- `validation/planning-browser-guard.json` passed source-write denial through browser runner in planning mode.
-2 focused adapter tests passed: original-test restoration/symlink rejection and statement-line coverage union.

### Calibration fixes (already made, do not re-debug)

- Browser initial UI is Don't use a server -> Start fresh. `window.Actual.getServerSocket()` gives the existing worker; ipcConnect is Electron-only. RPC listener must not overwrite app handler.
- Experimental checkbox is asynchronous, use click then expect checked. Virtualized report card requires repeated scrolling after async widget save.
- Yarn4 rejects CLI-injected YARN_NO_PROXY; uniform YARN_ENABLE_STRICT_SETTINGS=false ignores that unknown proxy setting. Offline/immutable still enforced.
- Direct Chromium in Codex sandbox crashes on macOS IPC. `browser_broker.py` is a controlled per-turn local runner: scripts within candidate, Node/Chromium under separate Seatbelt, candidate-only home reads, loopback network, read-only dependencies/Git, planning source-write denial, sanitized env. ACTUAL_BROWSER_RUNNER is passed to agent; scripts POST to /run. Browser output saved in node_modules/.cache/browser-runner. Reference code never accessible. Broker is stopped at each CLI turn end.
- Runtime temp now node_modules/.cache/actual-eval-tmp so whole-project lint doesn't scan generated sw.js. Prior .eval-tmp failures are calibration artifacts, not reference defects.
- No Docker. Full node_modules uses separate APFS CoW clones preserving workspace-relative symlinks; never symlink entire dependency directory.

## ACTIVE processes at last update

- **70226 COMPLETED successfully**: final end-to-end unchanged-candidate grader smoke. It creates candidate+regression replays and runs candidate tests+coverage, protected original tests, build, browser, type, lint, historical compatibility. Latest: candidate core exit0 40s and web exit0 53s. On success writes `validation/replay-smoke-gate.json`.
- **22529 COMPLETED with controller import error before any trial started**: fixed missing SPEC import, then launched replacement controller30810. DO NOT launch a duplicate controller. It has not printed START APPLICATION yet at this note's time. Check session/status files.
- All earlier validation/preflight/baseline/browser probe sessions completed, including64992,86408,58538,53091,13434,44301,30058,84881. Old reference server82286/session93698 was stopped. No reference server should be active during measured actors.

## Matrix execution

`run_study.py` requires validation/gate.json + replay-smoke-gate. Seeded order **openspec,gennady,plan,prompt**, concurrency1. Limits30min/call,4h/approach,20Mtokens,64dispatches,8questions. One fresh run each. It freezes explicit engine files and gate/runtime hashes under frozen-engine before first run, writes schedule/evaluation settings, updates study status. Never edit fingerprinted engine after launch. Incomplete existing trials require inspection, never silently restart. app_runner grades partial work on timeout/blocked outcomes.

`finish_app.py` review/report queue has now been launched; see latest tool result for session ID. Launch once after smoke/first trial starts; it waits for results, reviews all4 blindly in fresh Luna xhigh no-tool sessions and writes COMPARISON.md/MEASUREMENTS.json. Quality costs separate, workflow_seconds excludes setup+external grader. Its code is outside frozen execution fingerprint.

Remaining:
1. Poll70226 and22529; verify smoke passes and first trial starts. If errors, diagnose before any measured trial; do not count infrastructure calibration as an approach failure.
2. Launch finish_app.py queue once. Persist through all4 results; inspect errors/UI-selector incompatibility with additional clearly labeled QA if needed.
3. Audit runtime model/efforts/fingerprints/reference isolation, report metrics with limitations, update statuscomplete and docs.
4. Deliver original12-run and separate4-run findings with links after both studies complete.

Main files: adapter.py (replay, tests, coverage, browser, grade); runtime.py; isolation.py; browser_broker.py; app_codex.py; app_runner.py; app_workflows.py; app_fingerprint.py; run_study.py; validate.py; preflight.py; preflight_build.py; test_adapter.py; serve.mjs; browser_check.mjs; finish_app.py. README has methodology/caveats.

Update: controller **30810** printed START APPLICATION openspec. The engine/schedule are now frozen; do not edit fingerprinted files. Smoke gate passed, no regressions, all baseline commands pass. No other trials started yet.

Latest: original smoke70226 done PASS; initial controller22529 import failure fixed before trials; active measured controller **30810** now in OpenSpec propose after initial Luna medium user turn. Active review/report queue **68002**. `audit_app.py` added outside frozen engine. Current fingerprint checked unchanged. Finalizer must run audit and update status complete after all4 trial results + reviews. Latest stable-render screenshots inspected and show line correctly. Do not edit the engine after its frozen schedule.

Update around 02:25 Moscow: OpenSpec proposal completed in783.948s,6,021,080 input tokens,32,876 output tokens; simulated user approved without questions. Controller30810 now in **003-agent-apply**. Live apply rollout ID **01a08868-e326-7cc2-9d96-a063da634e76** (prior propose ID01a0885c-d520-7531-8bc5-4201d624b05f). Candidate has shared calculation/tests, spreadsheet data adapter, graph/range/report/card, settings/dashboard integration, and docs; own validation underway. No result yet, remaining3 queued.

Added outside frozen engine: status.py (read-only progress), QA_POLICY.md (pre-result locator/format-only adjudication policy), post_metrics.py (uniform added statement-start line coverage and diff scope). Updated finish_app.py source to include post-metrics, but running queue68002 loaded the OLD module; after it finishes, invoke fresh `from finish_app import report; report()` to regenerate with the added fields, without rerunning model reviews. audit_app.py now checks actual rollout model/effort metadata plus borrowed grading helper unchanged against original frozen engine. Fingerprinted execution files remain untouched after launch.

Review queue correction BEFORE any trial result/review: stopped idle queue68002 (PID72433), changed reviewer CWD from the approach-named run directory to `.eval-cache/actual-review-contexts/<digest>` to preserve blinding. Restarted queue as **1938**, which now loads the current finish_app.py including post-metrics. Do not use/poll68002 except to close its terminated session. Trial controller30810 and frozen engine unchanged. post_metrics.py caches results keyed by its own method SHA. No first candidate result yet at this update.

First application result recorded: **OpenSpec budget_exhausted**, apply CLI turn hit1800s cap (not quota). Workflow2599.782s (~43.3min), grading317.531s. Candidate core632/web608 pass (15 added tests), protected baseline0 regressions, build/typecheck pass, lint fails one actual/prefer-trans-over-t error in BalanceForecastCard.tsx195. Raw browser passes startup+experimental opt-in, then fails exact-case menu selector: oracle `Balance forecast`, candidate visibly `Balance Forecast`. Must adjudicate that binding after all measured actors finish; keep fixture/expected values unchanged. Do NOT run another candidate/reference server while a measured actor is active. Candidate grading tree/build and screenshot are under openspec/partial-evaluation. Historical private-module suites fail, as expected from different shared-calculator architecture. Changed-file mapped statement-line coverage19.23%; actor+simulator API-equivalent estimate0.64232932, partial usage after timeout.179 user words,70 read words,zero questions; human proxy5.49min excludes full spec/code reading.

Controller30810 moved on to **Gennady**. Queue1938 has started the first anonymous OpenSpec quality review. Run fresh `finish_app.report()` after queue completion anyway, because post_metrics.py's cache/hash addition was made after its current import (math is unchanged). No engine edits after freeze.

Methodology observation during Gennady discovery: its interview switched to Russian (upstream workflow behavior), and simulator answered in Russian. It has8 questions so far (through018-user-discover), including confirmations and architecture choice. It also elicited/accepted **separate series and minima per currency** for mixed-currency accounts, which is NOT in the common task.md. Record as workflow-induced scope expansion; do not reward that extra scope in common-task grading or claim identical final specifications. The initial brief and simulator policy were identical; downstream exchanges differ by approach. Simulator accepted recommended architecture A; do not describe its involvement estimate as measured real-expert labor. No intervention/feedback to measured actors.

OpenSpec anonymous review is now completed (result.json): maintainability3,readability3,test_quality2,performance3,security4,requirements_completeness3,ui_accessibility2. It explicitly recognizes the raw browser selector mismatch; flags imported_payee vs importedPayee mapping and possible monthly minimum marker mismatch. These are model findings, not yet independently confirmed product defects. Lint's one failure is prefer-Trans-over-t; no baseline regressions. Source review did not include lint log text, only exit status; don't imply a full dynamic audit.

Gennady discovery reached its predeclared8-question cap: turn019 asks a ninth confirmation of external-audit resolutions (`годится`) before writing scope spec. It has not implemented product code; controller30810 now grades its partial/unchanged candidate. Treat as question-budget censoring, not a measured defect in generated application code. Extra multi-currency scope and language drift still relevant involvement observations.

Review queue1938 was stopped idle (PID73161) BEFORE any no-code review. Added explicit N/A ratings for no product code (requirements completeness1, with deterministic reason; no fabricated model review); audit handles not-applicable.json. New active review/report queue **14383** now loads post_metrics hash caching and N/A handling. Source finish_app.py then gained a provisional-progress line; queue loaded before that minor display edit, so still regenerate report from fresh source after completion. Original trial engine remains frozen and untouched.

Plan trial completed preparation: five recorded CLI calls (~8.4min), including one question round (exact lowest daily balance/date retained in monthly summary; rolling30-day/3-month/6-month/1-year presets with fixed manual ranges; opening balance is actual selected-account posted balance, report filters apply to changes), then normal planning approval. Now **005-agent-implement** running under controller30810. Direct prompt still queued. Gennady result recorded: workflow644.447s,361 user words,2612 read words,8 delivered questions,25.418min human proxy; only specs/README.md and specs/infra-base/infra-base.spec.md changed. No product code; N/A quality record saved.

Continuation update: controller30810 remains active; plan implement005 hit30min cap and partial grading is underway (candidate/regression suites, build and types pass so far). Prompt queued. Current review queue14383. OpenSpec and plan locator-only QA scripts prepared under qa-scripts but NOT executed; must wait all4 result.json before any QA server. finish_app.py source now has optional separate browser_adjudication fields/table from runs/<approach>/adjudication/summary.json and QA_REPORT.md; fresh report() after queue ends required. No execution fingerprint edits.

Latest continuation: ALL measured agent workflows CLOSED; prompt001 hit1800s cap, controller30810 now grades its partial code (core tests pass so far). Review queue14383 alive, plan review complete. Plan build/type/lint all pass,0regressions,12 added tests; raw browser also exact-case menu mismatch. Prepared qa-scripts/{openspec,plan,prompt}.{mjs,json}; prompt uses explicit Jan1-Dec31 and Jan1-Mar31 dates because presets90/365 days differ. Common task only says future preset, so do NOT label that alone a requirement violation. No QA server started yet: adjudicate.py requires4 result files. Run each with candidate-tree, script, new runs/<a>/adjudication/attempt-01, adaptationsjson afterall4. adjudicate.py now records source digests before/after. finish_app.py reads optional adjudication/summary.json and links QA_REPORT.md (currently pending skeleton). Finalaudit now requires4 reviewed summaries, source integrity forattempts, report+QA_REPORT. Summary schema: reviewed:true,status:human-readable,feature_checks_passed:int,attempts:[relative path e.g. adjudication/attempt-01],notes:[...]. No-codeGennady gets unavailable noimplementation summary withoutreplay. Must fresh report() afterqueue completes (loadedoldsource), runaudit, updatestudycomplete. Noresetused, frozenengineuntouched.

FINAL: Both studies COMPLETE. Controller30810 and reviewerqueue14383 ended successfully. Appall4 resultfiles+qualitydispositions recorded. Three implementations hit30min cap; Gennady8questioncap/noimplementation. BrowserQA: openspecattempt01, planattempt02 (attempt01 hovercomparisonerror retained), promptattempt02 (attempt01 alreadypass;02layoutdiagnostics). Allthree7/7 common+extraMonthlypersistencepass, sourcehashesunchanged. Gennadynoimplementation0/7. QA_REPORT.md complete withOpenSpecstaleenddate, promptconfirmedlayoutoverlap (zero-heightinnerflexrow, allancestor scrollTop0). No candidatefixes. finish_app.report() regenerated fromlatestsource, study.jsoncomplete, READMEupdated. AUDIT.jsonpassed:allactualLunamodel/efforts,fingerprint,historyisolation,grades/reviews,QAintegrity. Noresetused. Do not rerun any trials or queues.
