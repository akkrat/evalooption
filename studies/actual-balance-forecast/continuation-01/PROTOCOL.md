# Additional-budget continuation

The user authorized continuing all four approaches on Actual Budget. This extends the original bounded runs; it is not four fresh trials and does not replace their results.

The source workspaces are copied with APFS copy-on-write, including their original snapshot commits, uncommitted implementations and workflow artifacts. Completed stages are skipped. The last unfinished agent session and existing simulated-developer session resume with an explicit new working directory. Gennady's pending ninth discovery question is delivered to its existing developer session before discovery resumes. Previously answered questions are retained.

Models remain gpt-5.6-luna: medium developer, xhigh agents and workflow workers. The original task, workflow versions, runtime, isolation and grading stay unchanged. Agents receive no evaluator test results, visual QA findings, independent review, reference code or suggested fixes. Each agent must inspect and finish its own work. Original reports, source workspaces and recorded turns are preserved; CLI session histories gain continuation turns with the same model and efforts.

Additional limits per approach: 60 minutes per CLI call, four hours of workflow time, 80 million input-plus-output tokens checked between calls, 32 additional questions/revisions per stage, and the existing 64-dispatch limit. The original call/question bounds caused the stop; these higher bounds permit further work while retaining a recorded stopping condition. Limit changes make this a separately reported continuation rather than an interchangeable observation in the original comparison. No quota-reset credit is used.

Reports must show original and incremental involvement, tokens, estimated cost and time separately, along with cumulative totals. Inherited stages and prior conversation are context, not newly charged work. Candidate code is independently regraded. Blind quality review and any equivalent-control browser replays are recorded separately after submission. No other candidate/reference preview server runs while a measured actor is active.

A real Luna xhigh calibration verifies resume relocation and denial of the former checkout before execution. Continuation code and settings are frozen before the first resumed trial. Original checksums must still match at final audit.
