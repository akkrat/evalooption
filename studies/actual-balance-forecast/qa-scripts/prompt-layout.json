[
  {
    "from": "name:'Balance forecast',exact:true",
    "to": "name:'Balance Forecast',exact:true"
  },
  {
    "from": "await page.getByRole('button',{name:'Monthly',exact:true}).waitFor();",
    "to": "await page.getByRole('button',{name:'Daily',exact:true}).click();\nawait page.getByRole('button',{name:'Monthly',exact:true}).click();\nawait page.getByLabel('Start date',{exact:true}).fill('2017-01-01');\nawait page.getByLabel('End date',{exact:true}).fill('2017-12-31');"
  },
  {
    "from": "const report=page.getByTestId('reports-page');",
    "to": "const report=page.locator('body');"
  },
  {
    "from": "await expect(report.getByText(amount,{exact:true}).first()).toBeVisible();",
    "to": "await expect(report.getByText('Lowest projected balance',{exact:true}).locator('..').getByText(amount,{exact:true})).toBeVisible();"
  },
  {
    "from": "await expect(report.getByText(new RegExp('Lowest (Point|Balance).*'+date,'i'))).toBeVisible();",
    "to": "const formatted={'2017-12-15':'Dec 15, 2017','2017-03-15':'Mar 15, 2017'}[date];\n await expect(report.getByText('Lowest projected balance',{exact:true}).locator('..').getByText(formatted,{exact:true})).toBeVisible();"
  },
  {
    "from": "await page.getByRole('button',{name:'Next 3 months',exact:true}).click();",
    "to": "await page.getByLabel('End date',{exact:true}).fill('2017-03-31');"
  },
  {
    "from": "await page.getByRole('button',{name:'Daily',exact:true}).waitFor();",
    "to": "await page.getByRole('button',{name:'Daily',exact:true}).waitFor();\nawait expect(page.getByLabel('Start date',{exact:true})).toHaveValue('2017-01-01');\nawait expect(page.getByLabel('End date',{exact:true})).toHaveValue('2017-03-31');"
  },
  {
    "after": "await save('posted-activity');",
    "additional_unscored_probe": "\n// Additional unscored probe: all candidates default to Daily, so persist non-default Monthly too.\nawait page.getByRole('button',{name:'Daily',exact:true}).click();\nawait page.getByRole('button',{name:'Monthly',exact:true}).click();\nawait page.getByRole('button',{name:'Save widget',exact:true}).click();\nawait expect(page.getByText('Dashboard widget successfully saved.',{exact:true}).first()).toBeVisible();\nawait page.reload();\nawait page.getByRole('button',{name:'Monthly',exact:true}).waitFor();\nawait assertMinimum('350.00','2017-03-15');\nrecord('additional-nondefault-granularity-persistence','Monthly is non-default and survives save/reload with unchanged 350 minimum on March 15; separate from the seven common checks.');\nawait save('monthly-persisted');",
    "reason": "Daily is each candidate default; an extra Monthly save/reload discriminates granularity persistence. Original seven checks and expected outcomes unchanged."
  },
  {
    "from": "await page.getByRole('button',{name:'Save widget',exact:true}).click();\nawait page.reload();",
    "to": "await page.getByRole('button',{name:'Save widget',exact:true}).click();\nawait expect(page.getByText('Dashboard widget successfully saved.',{exact:true}).first()).toBeVisible();\nawait page.reload();",
    "reason": "Wait for the visible save acknowledgment before reloading."
  },
  {
    "after": "await page.screenshot({path:path.join(output,name+'.png'),fullPage:true});",
    "additional_visual_diagnostic": "fs.writeFileSync(path.join(output,name+'-layout.json'),JSON.stringify(await page.getByLabel('Start date',{exact:true}).evaluate(el=>{const rows=[];for(let x=el;x;x=x.parentElement){const r=x.getBoundingClientRect(),s=getComputedStyle(x);rows.push({tag:x.tagName,rect:{top:r.top,bottom:r.bottom,height:r.height},scrollTop:x.scrollTop,clientHeight:x.clientHeight,scrollHeight:x.scrollHeight,overflow:s.overflow,flexShrink:s.flexShrink,display:s.display});}return rows;}),null,2));",
    "reason": "Inspect whether the partially clipped controls are due to scroll position or element geometry; no source or expected-outcome changes."
  }
]
